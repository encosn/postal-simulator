# postal-simulator — 우체통에서 받은편지함까지

## ① 앱의 목적과 대상 단원

같은 내용의 편지를 **같은 시각에** 두 가지 방법으로 부친다. 하나는 종이에 손으로 써서 우체통에,
하나는 화면에 입력해서 보내기 버튼으로. 두 편지가 상대에게 닿기까지 **어디를 거치고 얼마나 걸리는지**
나란히 따라가며, 실생활의 자료가 디지털로 바뀌면 무엇이 달라지는지 눈으로 보게 하는 것이 목적이다.

- **대상**: 중학교 정보 **2단원 데이터**
- **성취기준**: **[9정02-01]** 실생활의 데이터가 디지털 형태로 변환되어 활용되는 긍정적 가치를
  탐색하고, 다양한 데이터를 디지털 형태로 표현한다.
- **교과서 연결**: 47쪽 「해 보기 2」

## ② 기능 목록

### 노선도 두 줄 (동시에 달린다)

| | 종이 편지 (아날로그) | 전자 우편 (디지털) |
|:---|:---|:---|
| 단계 수 | **12단계** | **9단계** |
| 내가 하는 일 | 5단계 (편지지 사기 → 편지 쓰기 → 주소 쓰기 → 우표 붙이기 → 우체통에 넣기) | 5단계 (앱 열기 → 주소 입력 → 내용 입력 → 첨부 → 보내기) |
| 시스템이 하는 일 | 7단계 (수거 대기 → 우체국 접수 → 집중국 운송 → 자동 분류 → 간선 운송 → 순로 분류 → 집배원 배달) | 4단계 (메일 서버 → 주소 조회 → 인터넷 전송 → 받은편지함) |
| 전체 소요 | **2일 23시간 40분** | **86.88초** |

- **노선도의 아이콘을 누르면** 그 단계에서 실제로 무슨 일이 벌어지는지 **작은 장면(SVG 애니메이션)**이 뜬다.
  장면은 12 + 9 = 21개이고 모두 손으로 그린 인라인 SVG다 (이미지 파일이 하나도 없다).
- 진행 중인 단계는 노선도에서 커지고, 지나온 단계는 색이 찬다. 도착하면 도장이 찍힌다
  (종이 편지는 우체국 소인 모양, 전자 우편은 알림 배지 모양).

### 조작

| 버튼 | 하는 일 |
|:---|:---|
| **▶ 동시에 출발** | 두 줄을 동시에 재생한다 |
| **한 단계씩 →** | 한 단계만 진행한다 |
| **↺ 처음부터** | 모두 되돌린다 |
| **시간 축: 단계 관찰 / 실제 비율** | 「단계 관찰」은 모든 단계가 눈에 보이도록 화면 재생 시간을 고르게 늘린 것, 「실제 비율」은 실제 시간 비율을 그대로 압축한 것. **어느 쪽이든 시계에 뜨는 숫자는 실제 시간이다** |
| **속도 1× / 2× / 4×** | 재생 속도 |

> **「실제 비율」이 이 앱의 핵심 장면이다.** 이 축에서는 전자 우편이 거의 즉시 끝나고,
> 그 순간 종이 편지는 아직 **문구점에 가는 길**(전 과정의 0.03%)이다.

### 걸린 시간 비교 (아래쪽 기록판)

두 편지가 **모두 도착해야** 숫자가 채워진다 (그 전에는 `?` 로 가려 둔다 — 먼저 예상해 보게 하려고).

| 숫자 | 뜻 |
|:---|:---|
| **약 2,970배** | 전 과정에 걸린 시간 차이 |
| **약 286,000배** | **손을 떠난 뒤** 전달되는 시간 차이 (우체통에 넣은 뒤 2일 22시간 / 보내기를 누른 뒤 0.88초) |
| **0.03%** | 전자 우편이 도착한 순간 종이 편지가 지나온 거리 |

마무리 문장으로 「종이 편지가 쓴 시간의 대부분은 **기다림과 옮김**이었다 → 전자 우편에는 그 단계가
통째로 없다. 사람이 옮길 물건이 아니라 **데이터**이기 때문이다」와 「종이 편지 한 통이 가는 동안
전자 우편은 약 **2,969통**을 보낼 수 있다」가 나온다.

### 생각해 보기 4문항

1. 종이 편지 12단계 중 전자 우편에는 아예 없는 단계는 몇 개인가? 왜 사라졌을까?
2. 전자 우편이 빠른 이유가 '기계가 빨라서'뿐일까? 사람이 하던 일 중 무엇이 없어졌나?
3. 예전엔 아날로그였지만 지금은 디지털로 바뀐 것을 두 가지 더 찾아보기 (사진·지도·음악·시계·책…)
4. 빨라진 대신 **잃은 것**은 없을까? 손으로 쓴 편지에만 남아 있는 가치는?

질문 아래에 **「패들렛에 내 생각 올리기」 링크**가 있다 (2026-09-07 추가).
누르면 교사가 만든 패들렛 제출 양식이 **새 창**으로 열린다
(`https://padlet.com/encosn/submission-request/BjRPz6pr2aMwvQGg`).
**이 앱은 답을 받지도 저장하지도 않는다 — 링크로 보내기만 한다.** 학생 답은 패들렛에 쌓인다.
주소를 바꾸려면 `index.html` 의 `<a class="padlet" href="…">` 한 곳만 고치면 된다.

⚠️ **이 4문항은 화면에 적혀만 있고 채점·힌트가 없다.** 프로젝트 규칙(학습 피드백 기본 포함)과
다른 점인데, 2026-09-07 에 **일부러 지금 모습 그대로 올리기로 정했다** — 정답이 하나로 정해지는
질문이 아니라 모둠 이야기 거리에 가깝고, 먼저 수업에서 써 본 뒤 필요하면 보완하기로 했다.

## ③ 현재 개발 상태와 다음 할 일

### 상태 (2026-09-07, 1차)

- [x] `index.html` **한 파일**로 완성 (76KB). 스타일·스크립트가 모두 그 안에 인라인으로 들어 있다.
- [x] 외부 의존성 **0개**. 인터넷에서 받아 오는 것은 **구글 폰트뿐**이고,
      `'Malgun Gothic','맑은 고딕'` 대체 글꼴이 지정돼 있어 **교실 인터넷이 끊겨도 열린다.**
- [x] `fetch`·`import`·`localStorage`·`sessionStorage` 를 하나도 쓰지 않는다 →
      **학생 정보를 저장할 수단 자체가 없다** (개인정보 미수집 규칙을 구조적으로 지킨다).
- [x] 다크 모드 대응 (`prefers-color-scheme` + `[data-theme]`), 좁은 화면 대응, `prefers-reduced-motion` 대응
      (애니메이션을 끄면 각 장면이 **멈춘 그림 한 장으로도 읽히게** 그려 두었다).

### 1차 뒤 손본 것 (2026-09-07)

1. **맨 끝 「도착」 역이 눌리지 않던 것을 고쳤다.** 노선도의 마지막 역(종이 편지 「우편함 도착」,
   전자 우편 「받은편지함 도착」)은 원래 **일부러 클릭을 막아 둔 것**이었다
   (클릭 처리에 `if(idx >= L.steps.length) return;`, CSS 에 `.station--end{cursor:default}`).
   단계가 아니라 도착 표시라는 뜻이었는데, **겉모습은 다른 역과 똑같이 회색으로 흐려져 있어서
   「비활성화된 버튼」으로 보였다.** 사용자가 눌러 보고 고장으로 여긴 실제 사례가 있었다.
   → 이제 **누르면 끝까지 건너뛴다** — 도착 카드, 도장, 전 과정 시간이 뜨고,
   두 줄이 다 도착하면 아래 기록판 숫자도 공개된다. 마우스 올림 효과도 다른 역과 같아졌다.
   전자 우편만 먼저 도착시킨 경우 「기다림 안내」의 통 수도 함께 채워 준다.
   테두리를 점선으로 두는 것은 그대로다 — **단계가 아니라 도착점이라는 표시**이기 때문이다.

2. **머리말 문단이 왼쪽으로 치우쳐 보이던 것을 고쳤다.** `.lead` 에 `max-width:58ch` 가 걸려 있어
   넓은 화면에서 글이 왼쪽 절반에만 좁게 모여 있었다. 폭 제한을 없애 **제목(`h1`)과 같은 너비**를
   쓰게 하고, 네 문장을 각각 `<span>`(블록)으로 감싸 **한 줄에 한 문장씩** 읽히게 했다
   (`.lead > span{display:block;word-break:keep-all}`). 1440px 화면에서 네 문장 모두 한 줄에
   들어가는 것을 확인했다.

3. **강조하는 글자에 전용 색을 주었다.** `<b>` 가 본문과 같은 색(`--ink`)이라 굵기로만 구분됐다.
   `--em` 토큰을 새로 만들어 `.lead b` · `.verdict b` · `.q b` 에 걸었다.
   **밝은 화면 `#0F7A6C`, 어두운 화면 `#2FD9C0`.** 종이 편지(빨강 `--post`)·전자 우편(파랑 `--net`)
   어느 쪽과도 겹치지 않는 초록 계열로 골랐다 — 두 색 중 하나를 쓰면 **강조가 「이 줄은 아날로그
   이야기」로 잘못 읽힌다.** 밝은 바탕에서 명암비 4.7:1 이라 본문 글자로 써도 읽힌다.
   (`.waiting b`·`.stamp--net b` 는 강조가 아니라 **숫자 표시**라 그대로 두었다)
4. **「생각해 보기」 아래에 패들렛 제출 링크를 넣었다** (`.padlet`). 위 ② 기능 목록 참고.

5. **화면 아래 글도 한 줄에 한 문장씩으로 바꿨다** (머리말과 같은 규칙).
   - **결론 문단**(`.verdict`) — 문장 span 은 `updateBoard()` 가 만든다.
     **여기 글은 마크업이 아니라 자바스크립트가 만들므로**, 문구를 고칠 때 `<span>` 을 빠뜨리면
     그 문장만 앞 문장에 붙어 버린다. `.verdict p` 의 `max-width:66ch` 도 함께 없앴다
     (머리말과 같은 이유 — 넓은 화면에서 왼쪽으로 치우쳐 보였다).
   - **맨 아래 설명**(`.colophon`) — 예전에는 `<br>` 하나로 두 묶음을 나눴는데,
     문장마다 `<span>` 으로 바꾸고 「화면 속 시간」 줄에만 `.colophon__group` 을 붙여
     위와 한 칸 띄운다(그 `<br>` 을 대신한다).
   - 좁은 화면에서는 한 문장이 여러 줄로 넘어간다 — **한 줄에 두 문장이 오지 않는다**는 뜻이지
     문장이 반드시 한 줄에 들어간다는 뜻은 아니다. 375px 에서 가로 스크롤 0 을 확인했다.
   - **「생각해 보기」 질문 카드는 그대로 두었다.** 카드가 좁아서 문장을 끊으면 오히려 산만해진다.

### 어디서 왔는가

이 앱은 **Claude 아티팩트로 먼저 만들어 둔 것**(「우체통에서 받은편지함까지」, 2026-09-06)을
2026-09-07 에 이 프로젝트의 앱 폴더로 옮긴 것이다. 옮기면서 고친 것은 **`<head>` 뿐이다** —
아티팩트로 발행될 때 씌워지는 껍데기 안에서 `<title>`·글꼴 링크가 `<body>` 안에 들어가 있던 것을
`<head>` 로 옮기고, `lang="ko"` 와 설명 메타를 넣었다. **본문(스타일·마크업·스크립트)은 손대지 않았다.**

### 아직 안 한 것

- [ ] 실제 교실에서 써 보고, **「실제 비율」 축을 몇 분 동안 보여 줄지** 감을 잡기
      (종이 편지 완주에 화면 시간 42초를 쓰도록 잡혀 있다 — `REAL_MODE_ANALOG_SECONDS`)
- [ ] 교실 TV에서 글씨 크기 확인. 본문이 16px 이라 다른 앱(18~21px)보다 작다.
      키우려면 `body{font-size}` 하나가 아니라 `clamp()` 로 잡힌 제목·시계·막대 숫자가
      서로 물려 있으니 바꾼 뒤 겹침을 다시 볼 것.
- [ ] 「생각해 보기」에 답 쓰는 칸·예시 답안을 넣을지 수업 뒤 판단 (위 ⚠️ 참고)

## 실행 방법

```
cd C:\project_AI\postal-simulator
npm install     (처음 한 번)
npm start       → http://localhost:8090
```

- **`index.html` 을 더블클릭해도 그대로 열린다.** ES 모듈·`fetch`·번들러를 쓰지 않기 때문이다.
- **`npm run build` 가 없다.** 번들할 것이 없어서 일부러 뺐다 (`hub` 와 같은 이유).
  그래서 `dist/` 도 만들지 않는다.
- 단위 시험도 없다. 순수 로직이 `LANES` 배열의 숫자 더하기뿐이라 따로 떼어내지 않았다.

## 배포 — **`git push` 하나가 전부다**

| | |
|:---|:---|
| 저장소 | `encosn/postal-simulator` |
| 주소 | https://encosn.github.io/postal-simulator/ |
| 방식 | **브랜치(`main`) 루트 소스 직접 서비스 — 빌드·Actions 없음** |

`hub`·`spread-simulator` 와 같은 방식이다. GitHub Pages 가 저장소 루트의 `index.html` 을
그대로 내보내므로 **워크플로 파일이 없고, `git push` 하면 몇십 초 뒤 반영된다.**

> 이 방식은 `gh` 토큰에 `workflow` 권한이 없어도 되고, Actions 첫 실행이 실패할 일도 없다.
> 대신 **push 를 빠뜨리면 아무 일도 일어나지 않는다** — "Actions 가 알아서 하겠지" 하고
> 기다리지 말 것. (루트 CLAUDE.md 에 같은 취지의 경고가 있다)

배포한 뒤 **`hub/data.js` 2단원 `activities`** 에 항목이 있어야 메인페이지에 나온다. 자동으로 따라가지 않는다.
